{
  "devDependencies": {
    "node-sass": "^7.0.1",
    "nodemon": "^2.0.16"
  },
  

"scripts":{
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-sass": "node-sass --include-path scss sass/index.scss public/css/style.css",
    "watch-sass": "nodemon -e scss -x \"npm run build-sass\""
  }
}
